/*using System;
using System.Collections.Generic;
using System.Text;

namespace evalCsharp
{
    class Async
    {
        ReadFile readfile;
        readfile = new Async();

        srt tbl;
        tbl = new Async();

        Timer tp;
        tp = new Async();
        async void MAsync(srt tbl, Timer tp)
        {
            readfile.print_reply(tp, tbl);
        }
        readfile.test(tbl, tp);
            Task.Run(() => Async(tbl, tp));
    }
}
*/